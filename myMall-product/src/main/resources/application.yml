spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.10.131:3306/mymall_pms?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      serverAddr: 127.0.0.1:8848
      #      username: 'nacos'
      #      password: 'nacos'
      config:
        namespace: product
        group: dev
      discovery:
        server-addr: 127.0.0.1:8848
  config:
    import:
      - nacos:mymall-product.properties?refreshEnabled=true
  application:
    name: myMall-product
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 10000
